# Παράδοση εργασίας — energy-crm

Αντίγραψε ολόκληρο αυτό το αρχείο στην αρχή της νέας συνομιλίας.
Κατάσταση: 2026-08-11.

---

## 1. Πώς δουλεύουμε

Είσαι senior developer με 30 χρόνια εμπειρίας. Όλα με προσοχή, τίποτα
αρπακόλλα. Όταν κάτι δεν είναι σίγουρο, το λες αντί να το δηλώνεις έτοιμο.

**Κανόνες που έχουν συμφωνηθεί και δεν αλλάζουν:**

1. **Ποτέ ολόκληρο rewrite υπάρχοντος αρχείου.** Μόνο στοχευμένες αλλαγές στα
   σημεία που πρέπει — για οικονομία και για να φαίνεται καθαρά στο diff τι
   άλλαξε.
2. **Κάθε αλλαγή καταγράφεται στο `docs/CHANGELOG.md`** με το *γιατί*, όχι μόνο
   το τι. Το «τι» φαίνεται στο `git log`.
3. **Ανάλυση πριν την επιδιόρθωση.** Μία πλήρης ανάλυση αξίζει περισσότερο από
   πέντε τμηματικά μπαλώματα. Αυτό είναι ρητό παράπονο του πελάτη — έγινε το
   λάθος και δεν πρέπει να επαναληφθεί.
4. **Ονόματα μεταβλητών με νόημα**, ώστε να συντηρεί τρίτος. Πρόσεχε για
   συγκρούσεις ονομάτων.
5. **Ασφάλεια = #1 προτεραιότητα.** Το σύστημα κρατά ΑΦΜ, ΑΔΤ, διευθύνσεις,
   σαρωμένες ταυτότητες και υπογραφές. (IBAN **δεν** κρατά πια — αφαιρέθηκε
   2026-08-11, βλ. CHANGELOG.)
6. **Φορτίο:** ~2.000 ανοιχτές καρτέλες, **20-40 ταυτόχρονες αιτήσεις**
   ρεαλιστικά, 50-70 στην αιχμή. Να λαμβάνεται υπόψη σε **ό,τι** γράφεται —
   N+1 queries, indexes, locks, race conditions. Ξεχώριζε hot paths (REST που
   καλείται συνεχώς) από σπάνιες admin ενέργειες.

---

## 2. Το project

WordPress plugin: CRM για συνεργάτες πωλήσεων ενέργειας και κινητής στην
Ελλάδα. Leads, συμβόλαια, πελάτες, προμήθειες, ιεραρχία δικτύου, αυτόματη
συμπλήρωση PDF εντύπων ανά πάροχο, e-signing.

**Αρχιτεκτονική:** strangler refactor. Νέος PSR-4 κώδικας σε `src/`
(Http → Access → Persistence → Domain), παλιός procedural `ECRM_*` σε
`includes/`, `admin/`, `public/`, που φορτώνεται μέσω `src/Legacy/Loader.php`.

**Τεκμηρίωση στο repo — διάβασέ την πρώτα:**

- `docs/ARCHITECTURE.md` — διαρκείς κανόνες και αρχές
- `docs/CHANGELOG.md` — ιστορικό με το σκεπτικό κάθε αλλαγής
- `docs/TESTING.md` — οι δύο σουίτες και το στήσιμό τους
- `docs/ORIZON-TODO.md` — κλειστό· μένει ως πίνακας παραπομπών, γιατί σχόλια
  μέσα στον κώδικα δείχνουν στους αριθμούς του

**Repo:** `github.com/fotogeniss/energy-crm` — private, να μείνει private.

**Το ιστορικό καθαρίστηκε 2026-08-11 και δεν περιέχει πλέον προσωπικά δεδομένα.**
Δεκατέσσερα JPEG με στοιχεία άλλου πελάτη — `orizon_activation-1..9`,
`orizon_port-1..2` και οι παλιές εκδοχές των `orizon_family-1..3` — αφαιρέθηκαν
με `git filter-repo --strip-blobs-with-ids`. Λεπτομέρειες στο CHANGELOG.

Δύο πράγματα που πρέπει να ξέρεις γι' αυτό:

- **Κάθε SHA άλλαξε.** 133 commits ξαναγράφτηκαν. Αν βρεις σε παλιό σημείωμα ή
  σε chat ένα SHA που δεν επιλύεται, είναι από πριν το rewrite. Ο χάρτης
  παλιό→νέο σώθηκε στο `.git/filter-repo/commit-map` — δεν είναι υπό git, οπότε
  θα χαθεί με το επόμενο clean.
- **Αφαίρεση κατά blob, όχι κατά path.** Το `orizon_family` υπάρχει σε δύο γενιές
  με το **ίδιο όνομα αρχείου**: παλιά από συμπληρωμένη φόρμα, σημερινά από κενή.
  `--path` θα είχε σβήσει και τα σημερινά, από όλα τα commits, και θα το είχαμε
  ανακαλύψει στο πρώτο κατέβασμα συνδυαστικής Orizon.

---

## 3. Περιβάλλον — τρεις παγίδες που κοστίζουν ώρα

**Τρέχε τα πάντα από το «Site shell» του Local** (κουμπί δίπλα στο Site
folder, ανοίγει δικό του παράθυρο):

```
cd wp-content\plugins\energy-crm
composer check
```

Εκεί το PHP είναι **8.2.29 με sodium** — αυτό που τρέχει πραγματικά το site.
Σε απλό cmd το `php` του PATH είναι άλλη εγκατάσταση **8.2.12 χωρίς sodium**:
τα tests κρυπτογράφησης παραλείπονται και η ζώνη ώρας διαφέρει. Και τα δύο PHP
φανέρωσαν πραγματικά bugs, αλλά το έγκυρο είναι του Local.

**Το git.** Σε **PowerShell** δουλεύει κατευθείαν, χωρίς τίποτα. Σε σκέτο **cmd**,
αν δεν το βρίσκει:

```
set PATH=%PATH%;C:\Program Files\Git\cmd
```

Αυτή η γραμμή είναι σύνταξη του cmd και **αποτυγχάνει στο PowerShell** — εκεί
θα ήταν `$env:PATH += ";C:\Program Files\Git\cmd"`. Μην την αντιγράψεις τυφλά.

**Πολυγραμμικά μηνύματα commit: πάντα με `-F`, ποτέ με `-m`.** Το PowerShell τα
δέχεται με συνέχιση `>>`· το cmd.exe **όχι** — κρατά μόνο την πρώτη γραμμή και
προσπαθεί να εκτελέσει τις υπόλοιπες ως εντολές. Γράψε το μήνυμα σε
`.commit-msg.tmp.txt` (είναι ήδη στο `.gitignore`) και:

```
git commit -F .commit-msg.tmp.txt
```

Δουλεύει και στα δύο shells, και λύνει μαζί και τα ελληνικά.

**Ο hook ΔΕΝ τρέχει τη σουίτα integration.** `composer check` = phpcs + phpstan
(level 6) + **unit**. Ο pre-commit hook τρέχει αυτό και μπλοκάρει το commit αν
αποτύχει — **πρέπει να είναι πράσινο**, αλλά πράσινο commit **δεν** σημαίνει
πράσινη σουίτα.

Ο αποκλεισμός είναι σκόπιμος (βλ. `TESTING.md`: όποιος δεν έχει στήσει βάση
πρέπει να μπορεί να κάνει commit). Σε συνδυασμό όμως με τον κανόνα «κάθε βήμα
αφήνει τη σουίτα πράσινη» είναι παγίδα: στις 2026-08-11 μια ολόκληρη μετακόμιση
(`a6eadab`) έγινε commit χωρίς να την έχει δει κανένα integration test, επειδή ο
hook πέρασε. Για **κάθε** commit του βήματος 10, τρέξε πρώτα:

```
composer check:all
```

**Οι μεταβλητές της βάσης δοκιμών χάνονται με το παράθυρο.** Κάθε νέο Site shell
ξεκινά χωρίς αυτές, και τότε το `composer test:integration` βγάζει «No test
database named» — που **δεν μοιάζει με αποτυχία test**, μοιάζει με σφάλμα
ρύθμισης, και είναι εύκολο να το προσπεράσεις. Στο ίδιο παράθυρο με τα tests:

```
set ECRM_TEST_DB_NAME=energy_crm_tests
set ECRM_TEST_DB_USER=root
set ECRM_TEST_DB_PASSWORD=root
```

**Τα πλήθη της σουίτας δεν γράφονται εδώ.** Ζουν στο §6.0 του ίδιου αρχείου,
σε ένα σημείο. Γράφονταν και εδώ μέχρι τις 2026-08-16 και απέκλιναν τρεις
φορές: «74 integration» ενώ το §6.0 έλεγε 120, και μετά «164/120» ενώ η σουίτα
ήταν 199/186. Το ίδιο αυτό §3 προειδοποιούσε για το λάθος ενώ το έκανε.

*Μία παγίδα ανάγνωσης που μένει, όπου κι αν γραφτούν: τα δύο πλήθη assertions
είναι χωριστά, ένα ανά σουίτα. Δεν αθροίζονται.*

Προσοχή στα skipped: σε PHP **χωρίς** sodium παραλείπονται 6 tests, όλα
κρυπτογράφησης. Αν δεις «6 skipped», έτρεξες με το λάθος PHP και τα tests που
προστατεύουν τα ΑΦΜ **δεν εκτελέστηκαν**. Από το Site shell του Local πρέπει να
μείνει 1.

---

## 4. Τι έχει γίνει — ασφάλεια και GDPR

Όλα ολοκληρωμένα, ελεγμένα, committed (`3756048` και επόμενα).

| Κενό | Λύση |
|---|---|
| Η διαγραφή άφηνε πίσω IBAN, υπογραφές, leads, tasks, events | `src/Persistence/PersonalDataEraser.php` — όλοι οι πίνακες |
| Η εξαγωγή (Άρθρο 15) έδειχνε λιγότερα απ' όσα κρατάμε | `src/Persistence/PersonalDataExporter.php` |
| Οι δύο παραπάνω απέκλιναν μεταξύ τους | `src/Persistence/PersonalDataTables.php` — μία κοινή λίστα |
| Τα WordPress privacy tools απαντούσαν «δεν βρέθηκε τίποτα» | `src/Admin/PrivacyTools.php` |
| Παλιές σαρωμένες ταυτότητες πιθανώς δημόσιες | `src/Infrastructure/DocumentProtection.php` — cron ανά ώρα, παρτίδες 25, atomic claim |
| Rate limiting παρακάμψιμο με πλαστά headers | `src/Infrastructure/ClientIp.php` — trusted proxies, ανάγνωση αλυσίδας από δεξιά |
| Η συναίνεση δεν ανέφερε την AI επεξεργασία εγγράφων | `ECRM_Tracking::consent_text()` + φίλτρο `ecrm_consent_text` |
| PII σε καθαρό κείμενο | `FieldCipher` + `CustomerFields` + `ContractFields` + blind index |

**Κρυπτογράφηση — πώς δουλεύει:**

Στήλες `afm`, `adt`, `street`, `street_no`, `postal_code` του `customers`, και
οι προσωπικές τιμές μέσα στο `contracts.extra_json`. **Opt-in** με
`define('ECRM_ENCRYPT_PII', true)` στο `wp-config.php`· η ανάγνωση καταλαβαίνει
και καθαρό κείμενο και ciphertext, οπότε δεν υπάρχει ημέρα σημαία.

Επειδή η κρυπτογράφηση είναι τυχαιοποιημένη, το `WHERE afm = %s` δεν ταιριάζει
ποτέ. Γι' αυτό υπάρχει η στήλη **`afm_hash`** (keyed HMAC): κρατά ζωντανό τον
έλεγχο διπλοεγγραφών και την αναζήτηση με **πλήρες** ΑΦΜ. Η αναζήτηση με
**μερικό** ΑΦΜ χάνεται και δεν επανέρχεται.

Ισχύει μόνο για **νέες** εγγραφές — δεν υπάρχει backfill για τις υπάρχουσες.

**Ο κανόνας που διατρέχει τα πάντα:** `ProviderFormFields::isPersonalInput()`
είναι **default-deny** και χρησιμοποιείται σε τρία σημεία — διαγραφή, εξαγωγή,
κρυπτογράφηση. Πεδίο που δεν έχει ταξινομηθεί θεωρείται προσωπικό.

**Bugs που βρέθηκαν στην πορεία και διορθώθηκαν:**

- Σχόλια `--` μέσα στα `dbDelta` statements έσπαγαν **κάθε** ALTER. Αυτή ήταν
  η αιτία που το dbDelta «σιωπηλά αγνοούσε» αλλαγές και που υπήρχε το
  migration `EnsureLegacyColumns`. **Ποτέ ξανά σχόλια εκεί.**
- Οι κρυπτογραφημένες στήλες ήταν πολύ στενές (`afm VARCHAR(20)` για ~74
  χαρακτήρες ciphertext). Σε server χωρίς strict mode θα περιέκοπτε σιωπηλά =
  μόνιμη απώλεια ΑΦΜ. Migration `WidenEncryptedColumns`.
- `ContractTerm::endDate()` διάβαζε σε τοπική ώρα και έγραφε σε UTC: **κάθε
  ημερομηνία λήξης σε κάθε υπογεγραμμένη σύμβαση ήταν μία μέρα νωρίτερα** σε
  ελληνικό server.
- Το `FieldCipher` αποθήκευε **σιωπηλά καθαρό κείμενο** όταν έλειπε το
  libsodium — ψευδής αίσθηση προστασίας. Τώρα πετά `MissingCipher`.

**Σουίτα integration** (`composer test:integration`): πραγματικό WordPress σε
βάση μιας χρήσης. Καλύπτει scope enforcement, κρυπτογράφηση, διαγραφή. Το
bootstrap **αρνείται να τρέξει** αν η βάση δοκιμών είναι η ίδια με του site —
η βιβλιοθήκη δοκιμών κάνει DROP σε κάθε πίνακα.

---

## 5. Τι έχει γίνει — ORIZON (κινητή τηλεφωνία)

Ζητήθηκε ανακατασκευή από την αρχή, γιατί η κινητή δεν έχει σχέση με το ρεύμα.

**Τέσσερα έντυπα** στο `assets/forms/`, με χάρτες συντεταγμένων:

| Έντυπο | Κλειδί | Σελίδες |
|---|---|---|
| Σύμβαση | `orizon_mobile` | 11 |
| Αίτηση Φορητότητας | `orizon_portability` | 2 |
| Συνδυαστική (Family) | `orizon_family` | 4 |
| COMBO (κινητή + ρεύμα) | `orizon_combo` | 7 |

Τα υπόβαθρα είναι **κενές** φόρμες (`source_is_filled: false`). Τα προηγούμενα
ήταν φτιαγμένα από συμπληρωμένα έντυπα και κουβαλούσαν στοιχεία άλλου πελάτη —
γι' αυτό χρειάστηκε το commit `db24418`. Τα παλιά διαγράφηκαν, και στις
2026-08-11 αφαιρέθηκαν και από το git ιστορικό (§2).

*Το `db24418` έχει πλέον **κενό diff**: αφαιρούσε αρχεία που, μετά το rewrite,
δεν υπήρξαν ποτέ. Κρατήθηκε επίτηδες με `--prune-empty=never`, ώστε κάθε
αναφορά της τεκμηρίωσης να δείχνει κάπου.*

**Κανόνες του πελάτη:**

- Πρόγραμμα σε dropdown: `orizon 5GB`, `orizon 10GB + 5GB`, `orizon 40GB`,
  `orizon unlimited`. Μία σύμβαση για όλα.
- ΤΥΠΟΣ ΑΙΤΗΣΗΣ: **Φορητότητα** ή **Αριθμοδότηση**. Η Αριθμοδότηση τσεκάρει
  «ΝΕΑ ΣΥΝΔΕΣΗ» στο έντυπο· η ΑΝΑΝΕΩΣΗ δεν εμφανίζεται στην οθόνη.
- Έκπτωση: **είτε** Συνδυαστική (κινητή + κινητή) **είτε** COMBO (κινητή +
  ρεύμα), ποτέ και τα δύο. Δίνουν την **ίδια** τιμή για διαφορετικό λόγο.
- **Οι σελίδες 9-10 της σύμβασης μένουν τελείως λευκές, ούτε καν η κεφαλίδα.**
  Είναι ενσωματωμένο τμήμα φορητότητας· η φορητότητα φεύγει ως ξεχωριστό
  έντυπο μαζί με τη σύμβαση. Επιβεβαιωμένο ρητά.

**Τιμές** (επιβεβαιωμένες από τους πίνακες των εντύπων):

| Πρόγραμμα | Αρχική | Σκέτη σύμβαση | Με συνδυαστική |
|---|---|---|---|
| 5GB | 20 € | 15 € | 13 € |
| 10GB + 5GB | 25 € | 18 € | 16 € |
| 40GB | 30 € | 23 € | 19 € |
| unlimited | 35 € | 26 € | 23 € |

**Κώδικας που γράφτηκε:**

- `src/Domain/Forms/MobilePaperwork.php` — ποια έντυπα βγαίνουν
- `src/Domain/Forms/MobilePlans.php` — προγράμματα και τιμές
- `ECRM_FormFill::template_keys()` / `::fill_all()` — πολλά φύλλα ανά αίτηση
- `ECRM_REST::store_contract_pdf()` — αποθηκεύει όλα τα φύλλα (`contract` +
  `form_*`) και σβήνει και τα bytes των προηγούμενων
- Στη φόρμα: `request_type`, `mobile_offer`, και το τμήμα «Στοιχεία Μετρητή»
  απέκτησε `data-when-energy="power,gas"`

---

## 6. ΤΙ ΜΕΝΕΙ ΝΑ ΓΙΝΕΙ

**Μόνο ανοιχτά.** Ό,τι κλείνει σβήνεται από εδώ την ίδια στιγμή — δεν μένει
τσεκαρισμένο να μπερδεύει τον επόμενο. Το τι έγινε και **γιατί** ζει στο
`docs/CHANGELOG.md`, που είναι και ο λόγος που υπάρχει.

---

### 6.0 ΠΟΥ ΑΚΡΙΒΩΣ ΣΤΑΜΑΤΗΣΑΜΕ

**Την κατάσταση του working tree και του remote τη λέει το `git status -sb` σε
ένα δευτερόλεπτο. Μη τη γράψεις εδώ** — γράφτηκε τρεις φορές και σάπισε τρεις
φορές, δες παρακάτω.

*Ημερομηνία δεν μπαίνει στην επικεφαλίδα αυτής της ενότητας: γράφτηκε
«2026-08-14» πάνω από μέτρηση της 15ης. Την ημερομηνία τη λέει το
`git log --format=%ad`, και δεν σαπίζει.*

**Σουίτα: 199 unit** (1 skipped, 682 assertions) **+ 193 integration** (1244
assertions), phpcs **203** αρχεία και phpstan **126** καθαρά. Μέτρηση
2026-08-16 από την έξοδο του `check:all`, μετά τη διόρθωση του
cross-contamination (CHANGELOG 2026-08-16 (5)): το `ContractSaveMappingTest.php`
έφερε πρώτα +6 integration/+71 assertions/+1 αρχείο phpcs, και μετά +1 test
(#7) και +19 assertions. Το `tools/diagnose-contamination.php` **δεν** μετράει
πουθενά: το `tools/` δεν είναι στο scope ούτε του phpcs ούτε του phpstan.

*Τα δύο πλήθη εργαλείων έλεγαν εδώ 199 και 124. Σωστό ήταν το NEXT-SESSION, και
επαληθεύονται χωρίς να τρέξει τίποτα: το phpstan αναλύει `src` (125 αρχεία)
συν το `energy-crm.php` = 126· το phpcs τα ίδια συν `tests` 48 (47 + το νέο
`ContractSaveMappingTest.php`), `includes` 19 χωρίς το vendored `includes/lib`,
`admin` 6 και `public` 4 = 203.*

*Οι αριθμοί της σουίτας εδώ ήταν 194/158 με ημερομηνία 2026-08-13 ενώ το
NEXT-SESSION έλεγε 199/169 — δύο πλήθη σουίτας σε δύο αρχεία, ακριβώς το λάθος
που το ίδιο αυτό αρχείο προειδοποιεί ότι διορθώθηκε στις 2026-08-12.
Συγχρονίστηκαν 2026-08-14· τα πλήθη εργαλείων ξέφυγαν και συγχρονίστηκαν
2026-08-16.*

#### Η μέρα της 13ης Αυγούστου, με μια γραμμή το commit

| Τι | Πού βρέθηκε |
|---|---|
| Εννέα ψευδείς δηλώσεις στην τεκμηρίωση | πέρασμα επαλήθευσης με grep |
| Backfill κρυπτογράφησης (`PiiBackfill`, cron) | §6.0.1 — ο κώδικας έγινε |
| Email πελάτη χωρίς επικύρωση | τυπωμένο έντυπο |
| Αίτηση κινητής με Φορητότητα **και** Ανανέωση | τυπωμένο έντυπο |
| Πάγια εντολή άδεια σε 6 έντυπα Protergia ρεύματος | τυπωμένο έντυπο |

**Τα τρία τελευταία τα βρήκε ένα πραγματικό PDF, όχι η σουίτα.** Καμία από τις
τρεις δεν έσπαγε τίποτα: fill key χωρίς συντεταγμένη δεν σχεδιάζεται, δύο Χ σε
μια ομάδα δεν είναι σφάλμα, και άκυρο email αποθηκεύεται μια χαρά. Τύπωσε,
**κοίτα το χαρτί**, και μετά ψάξε την αιτία στον κώδικα. Το §6α.1 το λέει για τις
οθόνες· ισχύει και για το χαρτί.

#### Το τοπικό δεν είναι πια σε καθαρό κείμενο

**Το `ECRM_ENCRYPT_PII` είναι ΑΝΟΙΧΤΟ** στο `wp-config.php` του τοπικού, και το
backfill έχει τρέξει: 31 πελάτες και 3 σάκοι `extra_json` κρυπτογραφήθηκαν, το
`afm_hash` επαληθεύτηκε αμετάβλητο. Αν κάποια οθόνη δείξει `ecrm1:…`, είναι
διαδρομή ανάγνωσης που δεν περνά από `fromStorage()` — προϋπάρχον bug που μόλις
έγινε ορατό, όχι κάτι που έσπασε.

#### Το δίκτυο έχει όριο βάθους, και είναι 50

Το `NetworkRepository::computePath()` σταματά να ανεβαίνει μετά από 50 γονείς.
Ο φραγμός μπήκε στις 5 Αυγούστου, την επομένη των 22 καταρρεύσεων μνήμης, και
μέχρι τις 14 **δεν τον δοκίμαζε τίποτα** — το βαθύτερο δίκτυο σε όλη τη σουίτα
ήταν τρία επίπεδα.

**Η συνέπεια, τώρα γραμμένη ως assertion στο `NetworkDepthTest`:** πέρα από τα
50 επίπεδα το path ενός συνεργάτη **δεν ξεκινά πια από την πραγματική ρίζα**.
Κρατά τους 50 τελευταίους προγόνους του και κόβει τους από πάνω. Πρακτικά:
*όποιος κάθεται ψηλότερα από την τομή παύει να τον βλέπει.*

Δεν είναι bug — είναι το τίμημα του να μη τρέχει η αναζήτηση για πάντα. Αλλά
είναι όριο προϊόντος: δίκτυο βαθύτερο από 50 θέλει άλλο σχέδιο, όχι μεγαλύτερο
νούμερο. Σε πραγματικό δίκτυο πωλήσεων τα 50 επίπεδα δεν φτάνονται ποτέ, οπότε
η επιλογή είναι σωστή — αρκεί να είναι συνειδητή.

*Ο φραγμός συνοδεύεται από δύο ακόμα άμυνες που τώρα έχουν κάλυψη: ανίχνευση
κύκλου στην αλυσίδα γονέων (μία χειροκίνητη εγγραφή στη βάση αρκεί για να
δημιουργηθεί), και απόρριψη path με διπλό id.*

#### Απόδοση: μετρημένη, όχι εικαζόμενη (2026-08-14)

**Το Βήμα 9 δεν κόστισε τίποτα σε ταχύτητα.** Μετρήθηκε επειδή το frontend
έδειχνε να αργεί, και η πρώτη σύγκριση έλεγε 1,34 s έναντι 3,29 s
`DOMContentLoaded` — δηλαδή ότι το σπάσιμο σε modules μας κόστισε δύο
δευτερόλεπτα. **Ήταν θόρυβος.** Με διάμεσο πολλαπλών δειγμάτων:

| Μέτρηση | c4878fc (πριν τα modules) | f31945b (τώρα) |
|---|---|---|
| DOMContentLoaded | 1.340 ms | **1.094 ms** |
| REST `contracts`, διάμεσος 7 δειγμάτων | — | **134 ms** |
| TTFB | — | 862 ms |

Και το αποφασιστικό, που δεν χρειάζεται καν σύγκριση: **τα εννιά αρχεία
φορτώνουν παράλληλα σε 108-157 ms**. Ολόκληρο το γράφημα κοστίζει ~157 ms
πραγματικού χρόνου. Η ένωσή τους πίσω σε τρία αρχεία θα κέρδιζε δεκάδες
χιλιοστά, όχι δευτερόλεπτα.

**Ο κανόνας που βγήκε από αυτό, και κόστισε δύο γύρους:** *σε αυτό το μηχάνημα
μια μέτρηση δεν σημαίνει τίποτα.* Το ίδιο αίτημα `contracts` έδωσε 165 και 925
ms στην **ίδια** καταγραφή. Το phpcs έκανε 5 και 23 δευτερόλεπτα με ίδιο κώδικα.
Πάντα διάμεσος, ποτέ ένα δείγμα.

*Και το Local σε Windows δεν προβλέπει την παραγωγή: χωρίς ρυθμισμένο opcache,
με αργό σύστημα αρχείων και με τον Defender να σαρώνει ό,τι αγγίζουμε. Τα 134 ms
εδώ αντιστοιχούν τυπικά σε 30-80 ms σε Linux server.*

#### Το 502 ερευνήθηκε: δεν είναι ο κώδικας — είναι το Local

**Στις 2026-08-14, 16:45:51 τοπική ώρα, το CRM γύρισε `502 Bad Gateway`.**
Ερευνήθηκε με τα logs την ίδια νύχτα. Γράφεται εδώ ολόκληρο ώστε να μη
ξαναφοβηθεί κανείς το ίδιο συμβάν.

**Η απόδειξη είναι η απουσία.** Το `logs/php/error.log` έχει **μία μόνο
εγγραφή σε όλη τη 14η Αυγούστου** — το timeout της σουίτας στις 12:55:40 UTC.
Το 502 έγινε 13:45:51 UTC. **Τη στιγμή της κατάρρευσης η PHP δεν έγραψε
τίποτα.** Fatal, εξάντληση μνήμης, uncaught exception, ακόμα και το
`set_time_limit` — όλα γράφονται, και τα βλέπουμε αλλού μέσα στο ίδιο αρχείο.
Εδώ σιωπή.

**Ο `logs/nginx/error.log` λέει τι έγινε:**

```
WSARecv() failed (10054: An existing connection was forcibly closed
by the remote host) while reading response header from upstream
  upstream: "fastcgi://127.0.0.1:10093"
  upstream: "fastcgi://127.0.0.1:10094"      ← δεύτερος worker, ίδιο δευτερόλεπτο
no live upstreams while connecting to upstream   ← για ~8 δευτερόλεπτα
```

**Δύο workers πέθαναν ταυτόχρονα και μετά δεν έμεινε κανένας.** Ένα αίτημα δεν
σκοτώνει δύο workers μαζί. Έπεσε ολόκληρο το PHP-FPM και ξανασηκώθηκε μόνο του
— υπηρεσία του Local, σε επίπεδο Windows.

**Τρία ανεξάρτητα στοιχεία δείχνουν το ίδιο:** στις 2026-08-07 ο nginx
κατέγραψε `worker process exited with code C0000142` και `could not respawn
worker` (σφάλμα φόρτωσης DLL των Windows, όχι PHP). Στις 2026-08-12 η MySQL
«αρνήθηκε ενεργά τη σύνδεση». Και η κονσόλα του browser εκείνη την ώρα έδειχνε
`ERR_NETWORK_IO_SUSPENDED` και `ERR_NETWORK_CHANGED` — το μηχάνημα είχε
κοιμηθεί.

*Αρνητικό δεν αποδεικνύεται. Αυτό που λέγεται είναι: κανένα ίχνος δεν δείχνει
τον κώδικα, και πολλά δείχνουν το περιβάλλον. **Αν το 502 ξαναεμφανιστεί σε
πραγματικό Linux server, η έρευνα ξεκινά από την αρχή** — αυτό το συμπέρασμα
αφορά αυτό το μηχάνημα.*

#### Τι άλλο κρύβει το php error.log — ιστορικά, αλλά ένα θέλει επιβεβαίωση

| Τι | Πότε | Κατάσταση |
|---|---|---|
| **22 εξαντλήσεις μνήμης 256 MB, όλες στο `src/Access/NetworkPath.php`** | 4-5 Αυγ | **Θέλει επιβεβαίωση** |
| 578 σφάλματα σύνταξης SQL | 4 Αυγ | Ιστορικά |
| 80 `ArgumentCountError` + 20 `TypeError` | 10-11 Αυγ | Η περίοδος του σπασίματος· διορθωμένα |
| 5 `Maximum execution time` | 13-14 Αυγ | Δικά μας, από τη σουίτα· έκλεισαν στο `bb8fea4` |
| 2 `Cron event list could not be saved` | 11 Αυγ | Μικρό, ανοιχτό |

**Το πρώτο είναι το μόνο που έχει σημασία για την παραγωγή.** Οι 22 καταρρεύσεις
μνήμης ήταν όλες στο `NetworkPath`, δηλαδή ακριβώς το N+1 που έκλεισε το Βήμα 3
με το materialized path. Δεν έχει ξαναεμφανιστεί σε δέκα μέρες, **αλλά δεν έχει
δοκιμαστεί με βαθύ δίκτυο**. Αν επανέλθει, αυτό ρίχνει την PHP σε παραγωγή για
τον σωστό λόγο — και τότε το 502 θα είναι δικό μας. Αξίζει test με ιεραρχία
πολλών επιπέδων πριν πάει ζωντανά.

*Από τις 13 Αυγούστου και μετά το log είναι καθαρό, εκτός από τα timeouts της
σουίτας που κλείσαμε.*

#### Το frontend ελέγχθηκε για XSS και είναι καθαρό — μη το ξαναψάξεις

**Πέρασμα 2026-08-13 σε όλα τα `innerHTML`. Δεν βρέθηκε καμία τρύπα.** Γράφεται
εδώ ρητά ώστε να μη γίνει δεύτερη φορά: αρνητικό αποτέλεσμα που δεν καταγράφεται,
ξαναψάχνεται.

Τα μεγέθη: **78 σημεία `innerHTML`** (62 στο `ecrm-app.js`, 14 στο
`ecrm-form.js`, 2 στο `ecrm-litsa.js`) και **146 κλήσεις `esc()`**. Και τα τρία
αρχεία ορίζουν δικό τους `esc()` — καλύπτει `& < > "`. **Καμία άλλη οδός
εισαγωγής HTML**: μηδέν `insertAdjacentHTML`, `document.write`, `.html()`,
`outerHTML`, `eval`, `new Function`.

Κάθε δεδομένο πελάτη περνά από `esc()` — ονόματα, ΑΦΜ, κωδικοί, διευθύνσεις,
notes, τίτλοι εργασιών, σώματα ειδοποιήσεων, απαντήσεις Λίτσας, αποτελέσματα
αναζήτησης, προειδοποίηση διπλοεγγραφής. Ελέγχθηκαν ένα-ένα, μαζί με τις
συναρτήσεις που χτίζουν HTML αλλού και το επιστρέφουν (`dashboardHTML`,
`commissionsHTML`, `analyticsHTML`, `expandPanel`, `renderDetail`…).

**Τα τέσσερα που μοιάζουν εξαιρέσεις, και γιατί δεν είναι:**

| Σημείο | Γιατί είναι εντάξει |
|---|---|
| `form.js:412` — KB body ωμό, ενώ ο τίτλος περνά από `esc()` | Σκόπιμο. `wp_kses_post()` στην αποθήκευση και **μόνο** `manage_options` γράφει KB |
| `form.js:298` — `p.name`, `p.logo_url` ωμά | Μόνο `manage_options` γράφει παρόχους· το `logo_url` περνά από `esc_url_raw()` που κόβει εισαγωγικά και αγκύλες |
| `form.js:631` — `item.file.name` ωμό | Τοπικό `File` που μόλις διάλεξε ο ίδιος ο χρήστης. Self-XSS μόνο, καμία διαδρομή προς άλλον |
| `t.customer`, `t.assignee` ωμά μέσα στο `sub` | **Δεν έφταναν ποτέ**, και το από κάτω bug διορθώθηκε. Δες παρακάτω |

**Το τελευταίο έμοιαζε το πραγματικό και δεν ήταν.** Το `sub` χτίζεται ως
`[t.customer, link, t.assignee].filter(Boolean).join(' · ')` και μπαίνει ωμό,
ενώ δίπλα του τα `t.title` και `t.note` περνούν από `esc()` — ακριβώς το «λείπει
μόνο εδώ ενώ δίπλα δουλεύει» του §6β. Αλλά το `/tasks` επιστρέφει τις ωμές
γραμμές του `TaskRepository::search()`, που κάνει `SELECT t.*, c.code AS
contract_code, cu.first_name, cu.last_name, cu.company_name` — **δεν υπάρχει
κλειδί `customer` ούτε `assignee`**. Είναι `undefined`, το `.filter(Boolean)` τα
πετάει, μένει μόνο το `link` που χτίζεται με `esc()`.

*Άρα όχι XSS — αλλά ήταν **υπαρκτό bug UI**: ο υπότιτλος της εργασίας δεν
έδειχνε ποτέ πελάτη ή ανάδοχο, ενώ ο κώδικας προφανώς το ήθελε.*

**ΕΚΛΕΙΣΕ 2026-08-14.** Το σημείο δεν ζει πια στο `app.js` — οι Εργασίες
βγήκαν στο `ecrm-view-tasks.js` με το Βήμα 9, και το `app.js` κατέβηκε στις
251 γραμμές, οπότε η παραπομπή «app.js:1486» παραπάνω δείχνει σε κώδικα που
δεν υπάρχει. Ο υπότιτλος χτίζεται τώρα από τις `taskCustomer()`,
`taskAssignee()` και `taskSub()`, με `esc()` σε κάθε κομμάτι τη στιγμή που
μπαίνει, και το `TaskListPayloadTest` καρφώνει τις στήλες που τις
τροφοδοτούν.

**Επιβεβαιώθηκε σε οθόνη 2026-08-14:** εργασία φτιαγμένη από σύμβαση δείχνει
«ΚΩΝΣΤΑΝΤΙΝΟΣ ΝΙΚΑΣ · ORIZON-0036». *Η πρώτη δοκιμή έδειχνε κενό υπότιτλο και
έμοιαζε με αποτυχία — ήταν εργασία φτιαγμένη από τη φόρμα της ίδιας της οθόνης
Εργασίες, που σκόπιμα δεν στέλνει `contract_id`. Κενός υπότιτλος εκεί είναι το
σωστό. Το ξεκαθάρισε το `contract_id: null` στην απάντηση, όχι η εικόνα.*

#### Τι είναι πραγματικά το Βήμα 9, και τι δεν είναι

**Δεν είναι βήμα ασφάλειας.** Είναι συντηρησιμότητας: build step, σπάσιμο ενός
αρχείου 2096 γραμμών, τέλος στη συρραφή HTML με `+`. Η φράση «τέλος στο
χειροκίνητο `innerHTML`» στο roadmap **διαβάζεται ως ασφάλεια και δεν είναι** —
διορθώθηκε στο `ARCHITECTURE.md`.

**Το επιχείρημα ασφάλειας που όντως υπάρχει είναι άλλο.** Το frontend είναι
καθαρό σήμερα επειδή κάποιος **θυμήθηκε** `esc()` 146 φορές χωρίς να ξεχάσει
καμία. Τίποτα δεν το επιβάλλει: ούτε test, ούτε lint, ούτε τύπος. Ο επόμενος που
θα γράψει `renderKati()` και θα ξεχάσει ένα `esc()` σε όνομα πελάτη εισάγει
stored XSS σε σύστημα με ΑΦΜ, ΑΔΤ και σαρωμένες ταυτότητες — **και η σουίτα θα
μείνει πράσινη**, όπως έμεινε για τις τρεις ατέλειες που βρήκε ένα PDF.

*Η προτεινόμενη απάντηση δεν είναι το Βήμα 9, είναι φύλακας στο μοτίβο του
`RestRouteGuardTest`: PHP test στην υπάρχουσα σουίτα που σαρώνει τα
`public/assets/*.js`, βρίσκει κάθε `innerHTML` με interpolation, και απαιτεί ότι
όσα δεν περνούν από `esc()` είναι **ίσα με ρητή εγκεκριμένη λίστα** — τα τέσσερα
παραπάνω. Νέο σημείο χωρίς ανθρώπινη κρίση → κόκκινο. Μηδέν νέο toolchain.
Ψευδώς θετικά υπάρχουν (αριθμοί, τοπικά literals)· γι' αυτό είναι **έγκριση**,
όχι έξυπνη ανίχνευση.*

#### Ο `FileRepository` έχει πλέον δίχτυ — και η μέτρηση που το ζήτησε ήταν μισή

`FileRepositoryTest.php`, 14 tests, 105 assertions. Ο **`FileRepository`, 486
γραμμές τότε**, απέκτησε δίχτυ — και το σπάσιμο έγινε στις 2026-08-15. Πού
προσγειώθηκε και τι σκόπιμα **δεν** βγήκε: §6β.

**Η προειδοποίηση της προηγούμενης καταγραφής βγήκε σωστή, και άξιζε.** Το grep
ονομάτων έδινε «μία αναφορά, και είναι σχόλιο». Ο έλεγχος συμπεριφοράς έδειξε
κάτι άλλο: `attach()`, `latestPathOfKind()` και `purgeGenerated()` ήταν ήδη
καλυμμένες, με πραγματικά assertions, μέσα από το `ContractDocuments::store()`
στο `ContractDocumentsTest`. Το όνομά τους δεν φαινόταν επειδή περνούν από τον
wrapper. Χωρίς αυτόν τον έλεγχο θα γράφονταν τρία tests που υπήρχαν ήδη.

Χειρότερη περίπτωση από «καθόλου κάλυψη» ήταν η `purgeForContracts()`: το
`PersonalDataErasureTest` **την καλεί**, αλλά εκείνο το contract δεν έχει ποτέ
γραμμή αρχείου, οπότε η κλήση έβγαινε πάντα σε κενό σύνολο. Φαινόταν καλυμμένη
και δεν ήταν. *Μια κλήση δεν είναι κάλυψη αν τα δεδομένα δεν φτάνουν στον κώδικα
που μετράει.*

**Δύο ευρήματα από το γράψιμο του διχτυού:**

- **`replaceKind()` άφηνε ορφανά bytes** — έσβηνε τη γραμμή χωρίς `deleteBytes()`,
  σε αντίθεση με κάθε άλλη διαδρομή διαγραφής της κλάσης. Ο
  `SigningController::storeSignatureImage()` την καλεί σε **κάθε** επαναϋπογραφή,
  άρα ο πελάτης που υπέγραφε δεύτερη φορά άφηνε την πρώτη υπογραφή στον δίσκο,
  χωρίς καμία γραμμή να δείχνει σε αυτήν — αόρατη και στην `purgeOrphans()`, που
  ψάχνει κρεμασμένες γραμμές, όχι αζήτητα αρχεία. Ακριβώς το «μισό» που η
  επικεφαλίδα της κλάσης υπόσχεται ότι δεν γίνεται. **Διορθώθηκε** στο ίδιο
  commit, με regression test.
- **`purgeOrphans()` είναι σχεδόν απρόσιτη από κανονική ροή.** Το
  `files.contract_id` έχει `ON DELETE CASCADE` (`AddForeignKeys`), οπότε η
  διαγραφή contract παίρνει ήδη μαζί τις γραμμές αρχείων. Το σενάριο που
  υπηρετεί μένει μόνο για site όπου το FK δεν εφαρμόστηκε — κάτι που το migration
  καταγράφει και προσπερνά αντί να μπλοκάρει. Το test το αναπαράγει με
  `SET FOREIGN_KEY_CHECKS=0` γύρω από ένα insert· είναι ο μόνος τρόπος να τρέξει
  η ίδια της η SQL.

*Και μια παγίδα του schema: το `unprotectedCount()` ρωτά `protected = 0 OR
protected IS NULL`, αλλά το `EnsureLegacyColumns` ορίζει τη στήλη `TINYINT NOT
NULL DEFAULT 0`. Καμία γραμμή δεν μπορεί να έχει NULL, οπότε το μισό της
συνθήκης δεν δοκιμάζεται. Το πρώτο test το είχε αγνοήσει και το insert με NULL
απορρίφθηκε σιωπηλά — η `IntegrationTestCase` κρύβει τα σφάλματα της βάσης, οπότε
φάνηκε ως λάθος πλήθος και όχι ως αποτυχία εγγραφής. Αν ένα fixture δεν βγάζει το
πλήθος που περιμένεις, ρώτα πρώτα αν γράφτηκε.*

**Μη γράψεις εδώ σε ποιο SHA είναι το `origin/main`.** Γράφτηκε δύο φορές και
σάπισε δύο φορές: το `0e86221` («Contract lifecycle out of ECRM_REST») και μετά
το `8a6b07f`. Και τα δύο διαβάστηκαν ως «υπάρχουν απέσταλτα commits» ενώ δεν
υπήρχαν. Το `git status -sb` το λέει σε ένα δευτερόλεπτο και δεν σαπίζει ποτέ.

**Γράψε SHA για commit, όχι πλήθος commits.** Η γραμμή «Ν commits» έχει ήδη τρεις φορές
διαβαστεί ως κατάσταση που δεν ίσχυε. Το `git log --oneline -5` το λέει σωστά σε
ένα δευτερόλεπτο· ένας αριθμός γραμμένος από μνήμη όχι.

**Και μη συμπεράνεις remote από το decoration του `git log`.** Στις 2026-08-11
γράφτηκε εδώ «κανένα commit pushed», επειδή το `git log --oneline -6` έδειχνε
μόνο `(HEAD -> main)`. Το `origin/main` απλώς ήταν 15 commits πίσω, έξω από το
παράθυρο. Η εντολή που απαντά είναι `git status -sb` ή `git rev-parse
origin/main` — όχι το τι έτυχε να φαίνεται στις τελευταίες γραμμές.

**ΤΟ ΒΗΜΑ 10 ΕΚΛΕΙΣΕ. Το `class-ecrm-rest.php` δεν υπάρχει πια.**

| Commit | Τι |
|---|---|
| `31d71d1` | `ContractNotificationsTest` — 10 έλεγχοι, 61 assertions, πράσινο με την πρώτη |
| `a6eadab` | Οι `notify*` βγήκαν σε `ContractNotices` + `NotificationRepository::add()` |
| `7e23148` | Η `uplineOf()` από βρόχο σε `NetworkPath::ids( pathFor() )` |
| `b6d7b4c` | Οι τρεις καλούντες στράφηκαν, τα περιτυλίγματα σβήστηκαν |
| `be53c7e` | Η `can_manage_team()` διαγράφηκε — ήταν νεκρή, όχι μετακόμιση |
| `8b30986` | Και οι δέκα χρήσεις του `NS` στο `Router::NAMESPACE` |
| *τελευταίο* | Ο no-cache στον `Router`, το αρχείο σβήστηκε |

Το κριτήριο κρατήθηκε από την αρχή ως το τέλος: **καμία assertion δεν άλλαξε
ποτέ.** Το `ContractNotificationsTest` άλλαξε μία φορά, στο `b6d7b4c`, και μόνο
στο ποιον καλεί.

Ο χάρτης όλων των διαδρομών ζει πλέον στο `ARCHITECTURE.md`, ενότητα «Η HTTP
επιφάνεια» — μαζί με τις τέσσερις που δηλώνουν ακόμη legacy κλάσεις
(`ECRM_Tracking`, `ECRM_KB`, `ECRM_Assistant`).

**Ο `ContractRepository` σπάστηκε** — 930 → 405, σε πέντε commits, με το ίδιο
μοτίβο: δίχτυ πρώτα, νέες κλάσεις με το παλιό λεπτό περιτύλιγμα, μετά οι
καλούντες, τέλος η διαγραφή. Καμία assertion δεν άλλαξε σε κανένα από τα πέντε.

| Commit | Τι |
|---|---|
| `806b1d6` | 22 characterisation tests στις πέντε λίστες |
| `fbe38f1` | `ContractQueries` + `ScopeClause` |
| `b000951` | 19 characterisation tests στις γραφές |
| `c9a4c23` | `ContractDetails`, `ContractTransitions`, `WritableColumns` |
| `07590ce` | Οι δέκα καλούντες, τα έντεκα περιτυλίγματα σβήστηκαν |

*Η σουίτα εκείνη τη στιγμή: 164 unit + 120 integration. Ο τρέχων αριθμός είναι
στην αρχή του §6.0 — μη διαβάσεις αυτόν εδώ ως σημερινό. Δύο πλήθη σουίτας στο
ίδιο αρχείο ήταν ένα από τα εννιά λάθη που διορθώθηκαν στις 2026-08-12.*

**Το καθάρισμα του git ιστορικού έκλεισε ολόκληρο**, τοπικά και στο GitHub. Το
repo διαγράφηκε και ξαναφτιάχτηκε, και το καθαρό ιστορικό ανέβηκε ολόκληρο. Το
παλιό commit επιβεβαιώθηκε **μη ανακτήσιμο**: `git fetch origin d4d1442…` απαντά
«not our ref». Το αντίγραφο ασφαλείας και η λίστα blobs διαγράφηκαν.

**Η ασφάλεια αρχείων είναι επίσης καθαρή.** Η οθόνη GDPR δείχνει πράσινο: μηδέν
έγγραφα δημόσια προσβάσιμα, όλα σε προστατευμένη αποθήκευση. Ελέγχθηκε 2026-08-11
στο τοπικό, που είναι αυτό που θα γίνει παραγωγή.

**Το επόμενο, με σειρά προτεραιότητας:**

1. ~~**Cross-contamination στη φόρμα σύμβασης**~~ — **ΔΙΟΡΘΩΘΗΚΕ 2026-08-16**,
   CHANGELOG (5). Δύο αλλαγές: η `contractFrom()` παραλείπει σε edit κάθε
   στήλη που δεν στάλθηκε (αντί να τη γράφει σε default/NULL) και η
   `resolveCustomer()` κρατά το υπάρχον `customer_id`· το `openEdit()` καλεί
   `ECRMForm.reset()` πριν το `edit(c)`. Καμία από τις δύο δεν αρκούσε μόνη
   της. Καρφωμένο από τα tests #2 (ξαναγραμμένο ως regression) και #7 (νέο),
   και τα δύο κόκκινα στον παλιό κώδικα.

   **Ζημιά που είχε ήδη γίνει: καμία.** Το `tools/diagnose-contamination.php`
   (read-only, δεν τυπώνει προσωπικά δεδομένα) βρήκε 31 ορφανούς πελάτες που
   εξηγούνται 31/31 από διαγραμμένες πρόχειρες συμβάσεις — μεγαλύτερο id 39,
   υπάρχουσες 8. Μηδέν συμβάσεις χωρίς πελάτη, μηδέν μαζικά αδειάσματα. Δεν
   υπάρχει ακόμα site παραγωγής· το τοπικό είναι το μελλοντικό παραγωγικό,
   οπότε αυτή η μέτρηση είναι η μόνη που χρειαζόταν.

   *Προσοχή στην ερμηνεία: καθαρό αποτέλεσμα σημαίνει ότι το άδειασμα πεδίων
   δεν χτύπησε. Η μόλυνση τιμή-με-τιμή δεν αφήνει ίχνος στο audit και καμία
   μηχανή δεν τη βρίσκει αναδρομικά. Εδώ δεν έχει σημασία γιατί δεν υπήρξε
   ποτέ αληθινός πελάτης· σε site με πραγματικά δεδομένα θα είχε.*
2. **Το backfill της κρυπτογράφησης — ο κώδικας γράφτηκε 2026-08-12, μένει η
   εκτέλεση σε clone και μετά σε παραγωγή.** Το `PiiBackfill` είναι ωριαίο cron
   με το σχήμα του `DocumentProtection`, και η οθόνη GDPR δείχνει τι απομένει με
   κουμπί «Μετατροπή τώρα» για επιτηρούμενο πέρασμα.

   *Αυτό το σημείο ζητούσε **migration**. Ήταν λάθος και δεν έγινε έτσι: ένα
   migration καταγράφεται μόλις τρέξει, οπότε σε site με τη σημαία κλειστή —
   δηλαδή στην προεπιλογή — θα κατέγραφε τον εαυτό του ως ολοκληρωμένο χωρίς να
   κάνει τίποτα, και δεν θα ξανατρεχε ποτέ όταν αργότερα άνοιγε η κρυπτογράφηση.
   Το γιατί, ολόκληρο, στο `ARCHITECTURE.md`, «Το backfill δεν είναι migration».*

   **Πριν τρέξει στην παραγωγή, τρία που δεν είναι κώδικας:**

   - Επιβεβαίωσε ότι το `SECURE_AUTH_SALT` είναι ορισμένο στο `wp-config.php`
     εκεί. Αν λείπει, το `wp_salt()` πέφτει σε τιμή του πίνακα `options` — το
     κλειδί ταξιδεύει **μέσα στο dump** και η κρυπτογράφηση δεν προστατεύει από
     την απειλή που δηλώνει. Τότε το backfill δεν πρέπει να τρέξει καθόλου.
   - Τα salts μπαίνουν στη διαδικασία backup δίπλα στη βάση, και **ποτέ
     περιστροφή του `SECURE_AUTH_SALT`**. Σήμερα μια περιστροφή χάνει λίγες
     πρόσφατες τιμές· μετά το backfill χάνει **κάθε ΑΦΜ, ΑΔΤ και διεύθυνση**,
     σιωπηλά, ως κενό πεδίο. Το `FieldCipherTest` το τεκμηριώνει ως
     συμπεριφορά — εδώ είναι λειτουργικός κανόνας.
   - **Τρέξε το `tools/preflight-encryption.php` εκεί.** Κάνει και τους τρεις
     ελέγχους: salts, libsodium, και μετρήσεις ανά στήλη με έλεγχο ότι καμία
     γραμμή με ΑΦΜ δεν έχει κενό `afm_hash`. Επιστρέφει κωδικό 1 αν υπάρχει
     εμπόδιο, οπότε μπορεί να μπλοκάρει deploy script. **Δεν τυπώνει ποτέ salt**,
     μόνο αν υπάρχει.

     *Τα νούμερα δεν γράφονται πια εδώ — σάπιζαν. Στις 13 Αυγούστου ήταν «2 από
     τις 35 γραμμές με ΑΦΜ», την επομένη 3 από τις 36. Το εργαλείο τα λέει τη
     στιγμή που ρωτάς.*

     Ένας έλεγχος που το script κάνει και δεν ήταν στην αρχική λίστα: ψάχνει
     `secure_auth_key`/`secure_auth_salt` **στον πίνακα options**. Ακόμα κι αν οι
     σταθερές υπάρχουν σήμερα, τιμή εκεί σημαίνει ότι κάποτε το WordPress
     παρήγαγε δικό του salt — και αν υπάρχουν ήδη κρυπτογραφημένες γραμμές,
     **εκείνη η γραμμή είναι το κλειδί τους και δεν σβήνεται**.
3. **DPA με την Anthropic + Άρθρο 30** (§6α.3). Στέλνονται σαρωμένες ταυτότητες
   σε AI. Δεν είναι κώδικας και δεν είναι γκρίζα ζώνη.
4. **`ContractSaveController` 340** — η μέτρηση κάλυψης του 2026-08-16 (2) είπε
   **δεν υπάρχει δίχτυ**· τα έξι characterisation tests μπήκαν την ίδια μέρα
   (`ContractSaveMappingTest.php`, CHANGELOG 2026-08-16 (3)) και είναι
   πράσινα. Ένα από αυτά επιβεβαίωσε, όχι υπέθεσε, ότι κάθε επεξεργασία
   ξαναγράφει ολόκληρη τη γραμμή σε defaults/NULL αν δεν ξανασταλούν όλα τα
   πεδία — ίδιο εύρημα με το item 1 παραπάνω, από την πλευρά της PHP. **Το
   item 1 έκλεισε (CHANGELOG (5)), άρα το σπάσιμο δεν μπλοκάρεται πια από
   τίποτα**: υπάρχει δίχτυ επτά tests και η συμπεριφορά που θα διατηρήσει το
   σπάσιμο είναι πλέον η σωστή, όχι η καταστροφική. Σημείωση για όποιον το
   πιάσει: η `contractFrom()` μεγάλωσε με τη διόρθωση (μια συνθήκη ανά στήλη),
   και είναι ο προφανής υποψήφιος για εξαγωγή σε δικό του collaborator.

**Ποτέ μην τρέχεις git από το Linux VM του βοηθού** — το mount δεν επιτρέπει
unlink, οπότε ακόμη και ένα `git status` αφήνει `index.lock` που μπλοκάρει κάθε
επόμενη εντολή. Καθαρίζει με `del ".git\index.lock"` από τα Windows. Συνέβη ξανά
στις 2026-08-11.

### 6α. Εκκρεμότητες του πελάτη — όχι κώδικας

1. Δοκιμή του `ECRM_ENCRYPT_PII` στο clone site `crm-test`: καρτέλα σύμβασης,
   αναζήτηση με πλήρες ΑΦΜ, προειδοποίηση διπλοεγγραφής, συμπλήρωση PDF. Αν
   κάποιο δείξει `ecrm1:...`, ξέφυγε διαδρομή ανάγνωσης.
   **Τρεις τέτοιες βρέθηκαν και κλείστηκαν 2026-08-10** — όλες στην παραγωγή
   εγγράφων, καμία σε οθόνη: το αποθηκευμένο έντυπο παρόχου, το `signed_pdf`,
   και ο κοινός τους πρόγονος. Έγραφαν δικό τους αντίγραφο του query της
   `findDetailed()` χωρίς το `fromStorage()` που το κλείνει. Υπάρχει πλέον test
   που το πιάνει. Η οπτική δοκιμή μένει σκόπιμα: επιβεβαιώνει τις **οθόνες**,
   που κανένα integration test δεν βλέπει.
2. `ecrm_trusted_proxies` — μόνο αν το site είναι πίσω από Cloudflare.
3. DPA με την Anthropic + καταχώρηση της AI επεξεργασίας στο Άρθρο 30.
4. Τυπώνει η Protergia διαφορετικό **επαγγελματικό** έντυπο ανά τιμολόγιο; Τα
   οικιακά ξαναφτιάχτηκαν έτσι (τέσσερα `protergia_oik_*`)· τα επαγγελματικά
   έχουν ένα ενιαίο. Αν ναι, χρειάζονται τα έντυπα από σένα.
   *Μη ζητήσεις κώδικα γι' αυτό: ο κλάδος `$biz` υπάρχει (γραμμή 72 του
   `class-ecrm-formfill.php`) και το `protergia_he_biz.json` έχει 36 πεδία και 4
   σελίδες. Το HANDOVER έλεγε μέχρι τις 2026-08-10 ότι λείπει· έλεγε λάθος.*
5. Μια πραγματική **εξαγωγή χωρίς φίλτρα** από την οθόνη. Το σχήμα διορθώθηκε
   2026-08-10 και υπάρχει test, αλλά το test κρίνει μόνο ότι η αίτηση περνάει —
   όχι ότι το αρχείο βγαίνει σωστό.

### 6β. Κώδικας

- **Η σειρά δουλειάς που άδειασε τον μονόλιθο, για το επόμενο legacy αρχείο:**
  characterisation tests πρώτα, πάνω στον παλιό κώδικα, σε δικό τους commit·
  μετά νέες κλάσεις με το παλιό ως λεπτό περιτύλιγμα· μετά τα σημεία κλήσης·
  τέλος η διαγραφή. Κάθε βήμα αφήνει τη σουίτα πράσινη, και το κριτήριο
  επιτυχίας είναι ότι τα tests αλλάζουν **μόνο** στο ποιον καλούν.

  Δύο μαθήματα που κόστισαν, και τα δύο για την καταγραφή και όχι για τον κώδικα:

  - **Πολύ ΕΥΡΕΙΑ διατύπωση κρύβει bug.** «Λείπει από παντού» διαβάζεται ως
    εκκρεμότητα· «λείπει μόνο εδώ ενώ δίπλα δουλεύει» διαβάζεται ως bug. Έτσι
    έμεινε κρυμμένο για μήνες το άδειο κουτάκι της πάγιας εντολής στα Protergia
    ρεύματος, ενώ στο αέριο του ίδιου παρόχου τσεκαριζόταν κανονικά. Διορθώθηκε
    2026-08-13· η ίδια ευρεία διατύπωση είχε επιζήσει στο `PROTERGIA-FIELDS.md`
    μία μέρα παραπάνω από το HANDOVER.
  - **Πολύ ΣΤΕΝΗ διατύπωση κρύβει καλούντες.** Τρία έγγραφα έλεγαν «το
    `ECRM_Tracking` σε τρία `register_rest_route`» για το `NS`. Ήταν δέκα
    χρήσεις σε έξι αρχεία, και η διαγραφή του αρχείου με στραμμένο μόνο το
    Tracking θα έριχνε fatal σε κάθε `rest_api_init` — σε ολόκληρο το plugin.

  Ίδια ρίζα και στα δύο: αριθμός γραμμένος από μνήμη αντί από grep.
- **Βήμα 9: ΕΚΛΕΙΣΕ 2026-08-14, και δύο από τα τρία που υποσχόταν
  απορρίφθηκαν συνειδητά. Μη τα ξαναπροτείνεις.** Έγινε το σπάσιμο του
  `ecrm-app.js` (2096 → 251 γραμμές, 23 modules γύρω του, 24 αρχεία `.js`
  συνολικά) και ο φύλακας `esc()`. Το «build step» αντικαταστάθηκε από native
  ES modules με import map: κανένα build artifact, άρα κανένας τρόπος να σταλεί
  μπαγιάτικο. Το «τέλος στη συρραφή HTML με `+`» απορρίφθηκε ρητά στις
  2026-08-13 — 4292 γραμμές ξαναγραμμένες χωρίς κανένα JS test είναι μεγαλύτερο
  ρίσκο από αυτό που λύνουν. Και τα δύο είναι γραμμένα ως αποφάσεις κάτω από το
  roadmap του `ARCHITECTURE.md`.

  *Αυτή η γραμμή έλεγε μέχρι τις 2026-08-16 «Βήμα 9: frontend build step, τέλος
  στο χειροκίνητο innerHTML» — δηλαδή απαριθμούσε ως δουλειά ό,τι είχε
  απορριφθεί. Το `CHANGELOG 2026-08-13 (5)` καταγράφει τη φράση ως διορθωμένη·
  διορθώθηκε μόνο στο `ARCHITECTURE.md` και επέζησε εδώ σε δύο σημεία.*
- **Μεγέθη κλάσεων.** Το όριο είναι ~200 γραμμές. Ο `ContractRepository`
  σπάστηκε 2026-08-11 από 930 σε πέντε κλάσεις και προσγειώθηκε στις **402**
  (μέτρηση 2026-08-12)· το
  γιατί σταματά εκεί και δεν πάει στις 200 είναι γραμμένο στο `ARCHITECTURE.md`,
  ενότητα «Μεγέθη κλάσεων — και πού σταματά ο κανόνας». **Μη το ξαναπροσπαθήσεις
  νομίζοντας ότι ξεχάστηκε.**

  Απομένουν, από μέτρηση 2026-08-16: `PersonalDataEraser` **368** και
  `ContractSaveController` **340** (η καταγραφή έλεγε 324).

  Ο `FileRepository` ήταν 486 και **έκλεισε στις 320**: βγήκε το
  `UnprotectedDocuments` (182) και το `DocumentStorage` (66), με καμία
  assertion αλλαγμένη. **Σε δύο commits, όχι τρία** — σχεδιάστηκαν τρία και το
  τρίτο έγινε κατά λάθος `--amend` πάνω στο δεύτερο, οπότε το `6c7856e` έχει
  μαζί τη στροφή των καλούντων και τη διαγραφή. **Δεν πάει στις 200 και δεν
  πρέπει** — το `purging` θα απαιτούσε το `deleteBytes()` να γίνει κοινό,
  δηλαδή την ίδια αραίωση εγγύησης που σταμάτησε τον `ContractRepository` στις
  402. Δες `ARCHITECTURE.md`, «Μεγέθη κλάσεων».

  *Το «349» έζησε εδώ και στο `ARCHITECTURE.md` μέχρι τις 2026-08-16, ενώ το
  «320» ήταν γραμμένο σωστά τρεις γραμμές πιο κάτω, στο ίδιο bullet. Δύο
  αριθμοί για το ίδιο πράγμα στην ίδια παράγραφο δεν διαφωνούν θορυβωδώς —
  διαβάζεις τον πρώτο.*

  **Ο `PersonalDataEraser` ΔΕΝ σπάει, και ρωτήθηκε πριν αγγιχτεί** (2026-08-16).
  Ένα public `erase()`, έντεκα private, οκτώ από αυτές μία ανά πίνακα με
  προσωπικά δεδομένα. Είναι η πιο εύθραυστη από τις τρεις περιπτώσεις: εδώ την
  εγγύηση δεν τη φυλάει ούτε υπογραφή ούτε `private`, μόνο το ότι υπάρχει ένα
  αρχείο. Ολόκληρο το σκεπτικό στο `ARCHITECTURE.md`, «Μεγέθη κλάσεων».

  **Ο `ContractSaveController` είναι το αντίθετο, γι' αυτό είναι ο επόμενος.**
  Το docblock του λέει ρητά ότι οι κανόνες ιδιοκτησίας **δεν** επαναλαμβάνονται
  εκεί — κάθε ανάγνωση και γραφή περνά από repository που δεν τρέχει χωρίς
  scope. Η εγγύηση ζει αλλού. Αυτό που συγκεντρώνει είναι χαρτογράφηση
  αιτήματος → στήλες (`customerFrom`, `contractFrom`, `addressFrom`, 155 από
  τις 340 γραμμές), με διαφορετική διάρκεια ζωής από την ενορχήστρωση της
  `save()`. **Αρχίζει με μέτρηση κάλυψης συμπεριφοράς, όχι με grep ονομάτων** —
  το grep ονομάτων είχε πει «μία αναφορά και είναι σχόλιο» για τον
  `FileRepository` ενώ τρεις μέθοδοι ήταν ήδη καλυμμένες μέσα από wrapper και
  μία φαινόταν καλυμμένη χωρίς να είναι.

  **Η μέτρηση έγινε 2026-08-16, και το αποτέλεσμα είναι ότι δίχτυ δεν
  υπάρχει.** Ένα αρχείο αγγίζει τη διαδρομή, πέντε μέθοδοι, καμία assertion δεν
  διαβάζει αποθηκευμένη γραμμή· η `contractFrom()` γράφει 32 στήλες και οι δύο
  τιμές που φτάνουν από αίτημα ταυτίζονται με τα defaults του κώδικα. Ολόκληρο
  το σκεπτικό και οι νεκροί κλάδοι στο `CHANGELOG.md`, 2026-08-16 (2).

  **Τα έξι characterisation tests μπήκαν 2026-08-16**, σε δικό τους commit
  (`ContractSaveMappingTest.php`), στο μοτίβο του `RenewalsPayloadTest` —
  `assertSame` σε ΟΛΟΚΛΗΡΟ τον πίνακα στηλών, γιατί το `assertArrayHasKey` ανά
  ένα δεν πιάνει μετονομασία. Και τα έξι πράσινα (integration 186→192,
  +71 assertions) — αλλά το πράσινο του #2 **δεν** σημαίνει «όλα καλά».

  *Έγιναν επτά την ίδια μέρα: μετά τη διόρθωση (CHANGELOG (5)) το #2
  ξαναγράφτηκε ως regression test και προστέθηκε το #7 για τα address blocks
  — integration 192→193, 1225→1244 assertions. Η λίστα παρακάτω περιγράφει τι
  βρήκε το καθένα ΤΟΤΕ, που είναι και ο λόγος που υπάρχει.*

  1. Δημιουργία με κάθε κλειδί συμπληρωμένο, ανάγνωση της γραμμής πίσω.
     Πράσινο — καμία μετονομασία/swap ανάμεσα σε `supply_number`/`meter_number`.
  2. Επεξεργασία: αλλάζει ό,τι στάλθηκε, δεν πειράζει ό,τι δεν στάλθηκε.
     **Πράσινο με το αντίθετο ενδεχόμενο απ' ό,τι έλπιζε ο τίτλος**: η
     `contractFrom()` δεν διάβαζε ποτέ το `$existing`, άρα κάθε επεξεργασία
     ξανάγραφε ΟΛΗ τη γραμμή σε defaults/NULL — μαζί με `customer_id`, που
     μηδενιζόταν αν δεν σταλεί. Βλ. CHANGELOG 2026-08-16 (3)/(4).
     **Διορθώθηκε στην (5)**· το test κρατά τώρα τον τίτλο του κυριολεκτικά
     και κοκκινίζει αν η παλιά συμπεριφορά επιστρέψει.
  3. `addr_same = 1` και στα δύο προθέματα — τα πέντε μέρη **καθαρίζονται**.
     Πράσινο, όπως προέβλεπε ο κώδικας.
  4. `consent = 1` — γράφονται `consent_at` και `consent_ip`, αλλιώς κανένα.
     Πράσινο.
  5. Ξένο `customer_id` — 404 και **τίποτα γραμμένο**. Πράσινο.
  6. Ο σάκος `extra` ως πίνακας δίπλα σε πεδία διεύθυνσης — ο φύλακας
     `is_scalar` κάνει αυτό που λέει. Πράσινο.
  7. *(προστέθηκε με τη διόρθωση της (5))* Edit που δεν αγγίζει ένα address
     block το αφήνει άθικτο· edit που αγγίζει ΕΝΑ κλειδί του το ξαναϋπολογίζει
     ολόκληρο. Τα δύο μισά μαζί σε ένα test, γιατί το καθένα μόνο του
     επιτρέπει λάθος υλοποίηση: μόνο το πρώτο περνά και μια υλοποίηση που δεν
     γράφει ποτέ διεύθυνση, μόνο το δεύτερο και μια που τις γράφει πάντα.

  *Η πρόβλεψη ότι τα 3/4/5 ήταν τα πιο επικίνδυνα βγήκε λάθος — ήταν το 2, και
  ο λόγος δεν φαινόταν από τον τίτλο του, μόνο από την ανάγνωση της
  `contractFrom()` γραμμή προς γραμμή.*

  **Το `ecrm-form.js` δεν καθαρίζει state ανάμεσα σε δύο διαδοχικά edits,
  βρέθηκε 2026-08-16, όχι διορθωμένο.** Το `ECRMForm.init()` τρέχει μία φορά
  ανά page load — φρουρός `!loaded.form` στο `ecrm-app.js:69-71` — άρα το
  `state` της φόρμας είναι singleton για όλη τη συνεδρία. Το `openEdit()`
  (`ecrm-app.js:224-230`) καλεί `ECRMForm.edit(c)` απευθείας, ΧΩΡΙΣ
  `ECRMForm.reset()` — το `reset()` συνδέεται μόνο με το κλικ στο nav «Νέα
  Σύμβαση» (`ecrm-app.js:80-81`). Μέσα στο `applyEdit()`, το `setField()`
  (`ecrm-form.js:152-156`) και το `setChip()` επιστρέφουν χωρίς να κάνουν
  τίποτα όταν η νέα τιμή είναι κενή/null — δεν καθαρίζουν το input ή το chip.

  Αποτέλεσμα: αν ο συνεργάτης ανοίξει τη σύμβαση Α (π.χ. με `father_name`,
  `adt`, διεύθυνση, `price_type` συμπληρωμένα), και μετά, ΧΩΡΙΣ reload, τη
  σύμβαση Β που δεν έχει τιμή σε κάποιο από αυτά, το `collect()` στέλνει την
  παλιά τιμή της Α στο save της Β — και το εύρημα του item 2 παραπάνω σημαίνει
  ότι αυτή η τιμή πράγματι γράφεται στη Β. Δεν επιβεβαιώθηκε ακόμα με
  στιγμιότυπο οθόνης, μόνο με ανάγνωση κώδικα σε τρία αρχεία, γραμμή προς
  γραμμή· δεν υπάρχει JS test infrastructure (§3 του HANDOVER, `TESTING.md`) —
  ο έλεγχος θα χρειαστεί χειροκίνητο πέρασμα στην οθόνη ή ένα PHP test πάνω
  στη σειρά κλήσεων. Λεπτομέρειες: CHANGELOG 2026-08-16 (4).

  **Και μια παγίδα που κόστισε εκεί:** το `check:all` δεν αναλύει `admin/` και
  `includes/` — το phpstan τα έχει μόνο ως `scanDirectories`. Αλλαγή υπογραφής
  σε `src/` άφησε το `admin/class-ecrm-gdpr.php` να περνά λάθος τύπο, δηλαδή
  TypeError στην οθόνη GDPR **με τη σουίτα πράσινη πριν και μετά**. Όταν
  αλλάζεις υπογραφή, το grep στα legacy directories είναι μέρος της δουλειάς
  και δεν το αντικαθιστά κανένα εργαλείο.
- `birth_date`: δεν κρυπτογραφήθηκε, είναι στήλη DATE και θέλει αλλαγή τύπου.
  Η εκτίμηση ήταν ότι δεν αξίζει το ρίσκο· να συζητηθεί πριν γίνει.
- **Το GitHub καθαρίστηκε 2026-08-11 — αλλά διάβασε το γιατί, αν χρειαστεί ποτέ
  ξανά.** Επιλέχθηκε **διαγραφή και εκ νέου δημιουργία** του repo, όχι
  force-push. Μετά από force-push τα unreachable objects παραμένουν προσβάσιμα
  μέσω SHA σε caches και pull requests, και ο μόνος τρόπος να φύγουν είναι αίτημα
  στο GitHub Support — δηλαδή να εμπιστευτείς ότι καθάρισε κάτι που δεν μπορείς
  να ελέγξεις.

  Ήταν ασφαλές επειδή επιβεβαιώθηκε πρώτα: **0 forks**, private, 1 contributor,
  0 tags στο remote, καθόλου releases, κανένα issue ή PR. Τα forks είναι το
  κρίσιμο — μοιράζονται αποθήκευση αντικειμένων, οπότε ένα fork θα κρατούσε τα
  blobs ζωντανά ό,τι κι αν κάναμε.

  **Δύο παγίδες που χτυπήθηκαν στην πορεία.** Πρώτη: το `push` έγινε πριν τη
  διαγραφή του repo. Το tag πέρασε, το `main` απορρίφθηκε, και για λίγο το GitHub
  κρατούσε **δύο παράλληλα ιστορικά** — το παλιό στο `main` και το καθαρό
  κρεμασμένο από το tag. Δεύτερη: το git πρότεινε `git pull` για να «λυθεί», που
  θα κατέβαζε τα προσωπικά δεδομένα **πίσω** στο καθαρό τοπικό repo. Η σειρά
  είναι: διαγραφή πρώτα, push μετά. Ποτέ pull, ποτέ force.

  Το νέο repo πρέπει να φτιαχτεί **τελείως κενό** — όχι README, όχι .gitignore,
  όχι license. Οτιδήποτε από αυτά φτιάχνει αρχικό commit και το push απορρίπτεται
  ξανά.

  Επαλήθευση που αξίζει να επαναληφθεί: `git fetch origin <παλιό-sha>` πρέπει να
  απαντά «not our ref». Το `ls-remote` δείχνει μόνο refs· αυτό ρωτά για το
  αντικείμενο.

  **Όποιος έχει ήδη clone το κουβαλά, ανεξάρτητα από όλα τα παραπάνω.** Αν το
  repo δεν δόθηκε ποτέ σε τρίτο, αυτό το σενάριο δεν υπάρχει.
- **Το git δεν είναι η μόνη κρυψώνα, και είναι η μόνη που ελέγχεται εύκολα.**
  Στις 2026-08-11 βρέθηκε `wp-content/plugins/energy-crmcopy/` — πλήρες αντίγραφο
  του plugin, **έξω** από το repo, με τα παλιά `orizon_activation`, `orizon_port`
  και `orizon_family`, και τα τρία με `"source_is_filled": true`. Ζωντανά στον
  δίσκο. Διαγράφηκε.

  Το μάθημα: το `filter-repo` καθαρίζει **ιστορικό**, όχι δίσκο. Ψάξε με το
  κλειδί, όχι με το όνομα φακέλου — το `orizon_activation` δεν υπάρχει στο
  σημερινό plugin, άρα ό,τι το περιέχει είναι εξ ορισμού παλιό αντίγραφο. Το ίδιο
  ισχύει για backups: `wp-content/updraft/`, τα `*_backups/` του host, το `.trash`
  του File Manager, και ό,τι έχει κατέβει σε Downloads.

  Το `energy-crm.zip` **ελέγχθηκε και δεν είναι στο ιστορικό** — η παλιά
  ανησυχία έκλεισε.

---

## 7. Μέθοδος συντεταγμένων — μην την ξανα-ανακαλύψεις

Εγκεκριμένη από τον πελάτη, να μην αλλάξει:

1. `soffice --headless --convert-to pdf` το docx.
2. `pdftoppm -jpeg` → υπόβαθρα. **Η ανάλυση δεν είναι ενιαία και δεν πειράζει:**
   τα Orizon είναι 1191×1684 (144 dpi), τα Protergia/nrg/elpedison 1241×1754
   (150 dpi), και το `protergia_fa` **1241×1950 — δεν είναι καν A4**. Ο χάρτης
   κρατά χιλιοστά και το `page_w`/`page_h`, οπότε η απόδοση είναι ανεξάρτητη
   ανάλυσης. Το «1191×1684 (A4)» έγραφε εδώ ως καθολικό μέχρι τις 2026-08-13·
   ίσχυε μόνο για τα Orizon. Μέτρα το αρχείο, μη το υποθέσεις.
3. `pdftotext -bbox-layout` → θέσεις ετικετών (`mm = pt × 25.4 / 72`).
4. Θέση τιμής: `y = ymin + 0.78 × (ymax − ymin) − 2.5`, όπου 2.5 =
   `ECRM_FormFill::BASELINE`. Το `x` λίγο μετά το τέλος της ετικέτας.
5. Κουτάκια και στήλες πινάκων: **ανίχνευση γραμμών από την εικόνα**, «Χ» στο
   κέντρο του κουτιού.
6. Υπογραφές: με μετρημένη απόσταση από το επόμενο κείμενο της σελίδας.

**Ποτέ με το μάτι πάνω σε εικόνα. Πάντα από κενή φόρμα** — υπόβαθρο από
συμπληρωμένο έντυπο μεταφέρει τα δεδομένα του πελάτη που το υπέγραψε.
